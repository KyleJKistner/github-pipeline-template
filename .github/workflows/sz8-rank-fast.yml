name: Temporary Sz8 fast mwrank screen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fast-screen:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    container:
      image: sagemath/sagemath:10.9
    steps:
      - name: Placeholder
        run: echo "Automatic execution disabled; superseded by sz8-point.yml."
