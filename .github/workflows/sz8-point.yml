name: Temporary Sz8 point search (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy exploratory workflow disabled; use sz8-targets.yml.'
